package com.github.jblair.sqlmap.api.interfaces;

/**
 * Classifies each row from a result set in an API call. Intended to be applied to a record
 * from the configured outputResult package. The parameter names are bound to the column names, and
 * must match precisely.
 */
public interface APIOutputResult {}